# Next Steps - 2026-06-03

### 1. Research Platform Setup

1. Create the Zotero export at `/srv/ai/zotero/exports/library.bib`.
2. Place or link PDFs under `/srv/ai/zotero/pdfs`.
3. Parse source files into `/srv/ai/ingest/parsed`.
4. Chunk content into `/srv/ai/ingest/chunks` and `/srv/ai/ingest/metadata`.
5. Embed chunks with Ollama `bge-m3`.
6. Store vectors in Qdrant collection `research_chunks_bge_m3`.
7. Rerank retrieval candidates with `BAAI/bge-reranker-v2-m3`.

### 2. Benchmarking

1. Add a small benchmark set under `/srv/ai/rag/benchmarks`.
2. Record baseline retrieval output.
3. Record reranked retrieval output.
4. Compare citations and answer quality on known questions.

### 3. Backup And Validation

1. Run `scripts/backup_ai_stack.sh` after the first ingestion pass.
2. Confirm the backup archive is readable.
3. Rerun `scripts/validate_stack.sh` after any service or model change.

### 4. Notes

- Qdrant and Neo4j scaffolding already exist.
- Ollama models are stored under `/srv/ai/ollama`.
- The research bootstrap script is `scripts/setup_research_platform.sh`.
